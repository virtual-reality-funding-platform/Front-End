import React from 'react'

export const User = () => (
        <div>
            <h1>
                This is the component that will hold a each users information and imported into users.js
            </h1>
        </div>
    )

